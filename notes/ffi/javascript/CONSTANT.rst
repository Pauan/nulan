The ``CONSTANT`` statement creates a new constant in the current module.

It is represented with the JavaScript ``const`` statement:

* Nulan::

    (CONSTANT
    | foo :: Integer
    | foo <= 5)

* JavaScript::

    const foo = 5;
