The ``PROVIDE`` statement implements a protocol with a particular type and
zero or more constants. It exists only at compile-time, and has no effect on
the FFI.

(See ``REQUIRE`` for more information)
