(REWRITE-RULE
  (IMPORT-BUILTINS)
    &(STATEMENTS
       (IMPORT (nulan "boolean")
         | ~(SYMBOL Boolean)
         | ~(SYMBOL true)
         | ~(SYMBOL false)
         | ~(SYMBOL not)
         | ~(SYMBOL IF)
         | ~(SYMBOL AND)
         | ~(SYMBOL OR))))

(EXPORT
  | IMPORT-BUILTINS)



(LOAD-FILE "protocol.nul")
(LOAD-FILE "boolean.nul")

(EXPORT
  | Boolean
  | *true
  | *false
  | not
  | IF
  | AND
  | OR)

(EXPORT
  | Target
  | javascript-node
  | javascript-extension-chrome-client
  | javascript-extension-chrome-server
  | javascript-extension-client
  | javascript-extension-server
  | javascript-extension
  | javascript-browser
  | javascript
  | target-subset?
  | ($union Target))

(EXPORT
  |)
